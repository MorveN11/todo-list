{
  "name": "todo-list",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "dev": "turbo dev -F todo-list-db && turbo prisma -F todo-list-api && turbo dev -F todo-list-api -F todo-list-client",
    "check:code": "turbo lint -F todo-list-client -F todo-list-api && turbo format -F todo-list-client -F todo-list-api",
    "build": "turbo build -F todo-list-client -F todo-list-api",
    "test": "turbo test -F todo-list-client -F todo-list-api",
    "start": "turbo start -F todo-list-db && turbo prisma -F todo-list-api && node apps/todo-list-api/dist/src/main",
    "prisma": "turbo prisma -F todo-list-api",
    "up": "turbo dev -F todo-list-db"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "turbo": "^1.10.16"
  }
}
